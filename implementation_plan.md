# Deploy Streamor to Railway and Build Final Electron Executable

هوشمندی شما در ارائه توکن‌ها کار را بسیار سریع‌تر می‌کند! ما از طریق Railway CLI پروژه را مستقیماً از روی سیستم شما به فضای ابری منتقل می‌کنیم و سپس نسخه نهایی ویندوز (exe) را می‌سازیم.

## روند انجام کار (Implementation Plan)

### مرحله 1: دیپلوی مستقیم روی Railway
1. نصب Railway CLI به صورت گلوبال روی سیستم شما (`npm i -g @railway/cli`).
2. احراز هویت با استفاده از توکن Railway که ارسال کردید.
3. ساخت یک پروژه جدید در Railway و انجام دیپلوی اولیه (`railway up`). این کار کدهای بک‌اند (Next.js + Socket.io) را روی سرور ابری اجرا می‌کند.
4. دریافت دامنه ابری اختصاصی (مانند `https://streamor-production.up.railway.app`).

### مرحله 2: اتصال نرم‌افزار به سرور ابری
1. تغییر آدرس سرور سیگنالینگ (`SOCKET_URL`) در کدهای کلاینت تا به جای `localhost` به دامنه Railway متصل شوند.
2. آپدیت فایل `electron/main.ts` تا نرم‌افزار الکترون در زمان اجرا، مستقیماً آدرس ابری را لود کند (این کار باعث می‌شود هر زمان سایت را آپدیت کردید، نرم‌افزار کاربران هم به طور خودکار آپدیت شود).

### مرحله 3: گرفتن خروجی نهایی (Windows Build)
1. اجرای `electron-builder` برای ساخت فایل نصبی `.exe`.
2. آماده‌سازی فایل برای تست و استفاده.

> [!NOTE]
> در مورد **توکن گیت‌هاب**: برای دیپلوی در Railway ما نیازی به واسطه‌گری گیت‌هاب نداریم و می‌توانیم کدها را مستقیماً (Direct Upload) آپلود کنیم که بسیار سریع‌تر است و درگیر تنظیمات SSH و ریپازیتوری نمی‌شویم. اما اگر اصرار دارید کدها حتماً در گیت‌هاب هم آرشیو شوند، لطفاً تایید کنید تا مرحله گیت‌هاب را هم اضافه کنم.

## User Review Required
آیا با این مسیر دیپلوی مستقیم به Railway و سپس بیلد نرم‌افزار الکترون موافق هستید؟ روی گزینه **Proceed** کلیک کنید تا عملیات را استارت بزنم.
